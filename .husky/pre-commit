#!/usr/bin/env sh
. "$(dirname "$0")/_/husky.sh"

# 🔧 MASTER FLOW: Enhanced pre-commit with env checking
node scripts/ci-env-check.js
pnpm run guard:unified
pnpm run guard:gemini
pnpm lint:all || true
pnpm test:unit --passWithNoTests || true
